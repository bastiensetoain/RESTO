{% extends'base.twig' %}


{% block content %}

<main>
 
  
  <div class="w80 m-auto">
  
  
    
    <section class="blog">
        <h2 class="flex center-h flex-start">{{ post.title }} <hr></h2>
        <div class="plats">
            {% for post in plats %}
                    <div class="w50 flex font-s_8 around center-h">
                       <div>
                            <h3>{{ post.title }}</h3>
                            <p>{{ post.content }}</p>
                        </div>
                        <hr>
                        <p>{{post.get_field('prix')}} €</p>
                    </div>
            {% endfor %}
        </div>
    </section>
    
    </div>
    
</main>

{% endblock %}