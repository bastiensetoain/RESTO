{% extends'base.twig' %}


{% block content %}

<main>
 
  
  <div class="w80 m-auto">
  
  
    
    <section class="blog">
        <h2 class="flex center-h flex-start">{{ post.title }} <hr></h2>
        <div class="flex row wrap center center-h">
            {% for post in plats %}
                    <div class="plat w45 flex font-s_8 between center-h pl-1 pr-1">
                       <div>
                            <h3>{{ post.title }}</h3>
                            <p>{{ post.content }}</p>
                        </div>
                        <hr>
                        <p class="prix">{{post.get_field('prix')}} €</p>
                    </div>

            {% endfor %}
        </div>
    </section>
    
    </div>
    
</main>

{% endblock %}