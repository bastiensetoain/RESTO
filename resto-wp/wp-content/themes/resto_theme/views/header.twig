<header class="flex around center-h">


    <a href="{{ site.url }}"><img src="{{ logo.0 }}" alt=""></a>

    <nav class="nav flex w40">

        {% for item in headermenu.get_items %}

            <a href="{{ item.get_link }}">{{ item.title }}</a>

        {% endfor %}

    </nav>


</header>